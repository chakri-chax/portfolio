{
  "installCommand": "npm install && npm install --prefix client",
  "buildCommand": "CI=false NODE_OPTIONS=--openssl-legacy-provider npm run build --prefix client",
  "outputDirectory": "client/build",
  "rewrites": [{ "source": "/contact", "destination": "/api/contact" }]
}
